{
  "name": "psico-manager",
  "version": "0.0.0",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "npm run dev -w packages/web",
    "build": "npm run build -w packages/shared && npm run build -w packages/web",
    "lint": "npm run lint -w packages/web",
    "test": "npm run test -w packages/shared && npm run test -w packages/web"
  }
}
